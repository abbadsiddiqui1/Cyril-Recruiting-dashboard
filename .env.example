# Copy this file to `.env` (which is gitignored) and fill in your values.
# start.sh loads `.env` automatically.

# Resend API key for sending reminder emails. Optional — the app runs without it,
# only email sending needs it. Get one at https://resend.com
RESEND_API_KEY=re_your_key_here
